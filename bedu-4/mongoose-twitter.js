let mongoose = require('mongoose')
let Schema = mongoose.Schema
mongoose.connect('mongodb://localhost:27017/nueva-db')

// Require de Models

// PROMT accion a realizar [registro, tweet, ver tweets]

// IF o SWITCH para cada accion

// En cada accion promts y metodos para accesar o guardar en la base de datos